\chapter{First Appendix}
\label{First appendix}
\phantomsection

\section{Linux Kernel Memory Model}

% Highlighting
\sethlcolor{yellow}
\newcommand{\supported}[1]{\hl{#1}}

% Listings setup (allows LaTeX inside code)
% Subtle PLDI-style background color
\definecolor{codebg}{RGB}{250,250,245} % very light warm gray

% Listings setup (allows LaTeX inside code)
\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  columns=fullflexible,
  escapeinside={(*@}{@*)}
}



In this appendix, we present the \texttt{linux.bell} and \texttt{linux.cat} files of the Linux Kernel Memory Model (LKMM). 
We highlight in \texttt{yellow} the parts that are supported in our formalization.

\subsection{linux.bell}
\label{app:cat-bell}

\begin{lstlisting}
// SPDX-License-Identifier: GPL-2.0+
(* Linux-kernel memory consistency model *)

(*@\supported{enum Accesses =}@*)
    (*@\supported{'ONCE ||}@*)
    (*@\supported{'RELEASE ||}@*)
    (*@\supported{'ACQUIRE ||}@*)
    (*@\supported{'NORETURN ||}@*)
    (*@\supported{'MB}@*)

(*@\supported{instructions R[Accesses]}@*)
(*@\supported{instructions W[Accesses]}@*)
(*@\supported{instructions RMW[Accesses]}@*)

(*@\supported{enum Barriers =}@*)
    (*@\supported{'wmb}@*) ||
    (*@\supported{'rmb}@*) ||
    (*@\supported{'MB}@*) ||
    (*@\supported{'barrier}@*) ||
    'rcu-lock ||
    'rcu-unlock ||
    'sync-rcu ||
    (*@\supported{'before-atomic}@*) ||
    (*@\supported{'after-atomic}@*) ||
    (*@\supported{'after-spinlock}@*) ||
    (*@\supported{'after-unlock-lock}@*) ||
    'after-srcu-read-unlock

(*@\supported{instructions F[Barriers]}@*)

(* Filtering *)
let (*@\supported{FailedRMW}@*) = RMW \ (domain(rmw) | range(rmw))
let (*@\supported{Acquire}@*)   = ACQUIRE \ W \ FailedRMW
let (*@\supported{Release}@*)   = RELEASE \ R \ FailedRMW
let (*@\supported{Mb}@*)        = MB \ FailedRMW
let (*@\supported{Noreturn}@*)  = NORETURN \ W

(* SRCU *)
enum SRCU = 'srcu-lock || 'srcu-unlock || 'sync-srcu
instructions SRCU[SRCU]

let Srcu = Srcu-lock | Srcu-unlock | Sync-srcu

(* RCU matching *)
let rcu-rscs = let rec
    unmatched-locks   = Rcu-lock \ domain(matched)
and unmatched-unlocks = Rcu-unlock \ range(matched)
and unmatched         = unmatched-locks | unmatched-unlocks
and unmatched-po      = [unmatched] ; po ; [unmatched]
and unmatched-locks-to-unlocks =
    [unmatched-locks] ; po ; [unmatched-unlocks]
and matched = matched |
    (unmatched-locks-to-unlocks \
     (unmatched-po ; unmatched-po))
in matched

flag ~empty Rcu-lock \ domain(rcu-rscs)   as unmatched-rcu-lock
flag ~empty Rcu-unlock \ range(rcu-rscs)  as unmatched-rcu-unlock

(* SRCU matching *)
let carry-srcu-data = (data ; [~ Srcu-unlock] ; rf)*
let srcu-rscs =
    ([Srcu-lock] ; carry-srcu-data ; data ; [Srcu-unlock]) & loc

flag ~empty Srcu-lock \ domain(srcu-rscs)   as unmatched-srcu-lock
flag ~empty Srcu-unlock \ range(srcu-rscs)  as unmatched-srcu-unlock
flag ~empty (srcu-rscs^-1 ; srcu-rscs) \ id
    as multiple-srcu-matches

flag ~empty rcu-rscs & (po ; [Sync-srcu] ; po)
    as invalid-sleep

flag ~empty different-values(srcu-rscs)
    as srcu-bad-value-match

(* Marked vs plain accesses *)
let (*@\supported{Marked}@*) =
    (*@\supported{(\textasciitilde{}M) | IW | ONCE | RELEASE | ACQUIRE | MB | RMW |}@*)
    (*@\supported{LKR | LKW | UL | LF | RL | RU |}@*) Srcu-lock | Srcu-unlock

let (*@\supported{Plain}@*) = M \ Marked

(* Dependency propagation *)
let (*@\supported{carry-dep}@*) = (data ; [~ Srcu-unlock] ; rfi)*
(*@\supported{let addr = carry-dep ; addr}@*)
(*@\supported{let ctrl = carry-dep ; ctrl}@*)
(*@\supported{let data = carry-dep ; data}@*)

flag ~empty (if "lkmmv2" then 0 else _)
    as this-model-requires-variant-higher-than-lkmmv1
\end{lstlisting}

\subsection{linux.cat}
\label{app:lkmm-cat}

\begin{lstlisting}

// SPDX-License-Identifier: GPL-2.0+
(*
 * Copyright (C) 2015 Jade Alglave ,
 * Copyright (C) 2016 Luc Maranget  for Inria
 * Copyright (C) 2017 Alan Stern ,
 *                    Andrea Parri 
 *
 * An earlier version of this file appeared in the companion webpage for
 * "Frightening small children and disconcerting grown-ups: Concurrency
 * in the Linux kernel" by Alglave, Maranget, McKenney, Parri, and Stern,
 * which appeared in ASPLOS 2018.
 *)

"Linux-kernel memory consistency model"

(*
 * File "lock.cat" handles locks and is experimental.
 * It can be replaced by include "cos.cat" for tests that do not use locks.
 *)

include "lock.cat"

(*******************)
(* Basic relations *)
(*******************)

(* Release Acquire *)
(*@\supported{let acq-po = [Acquire] ; po ; [M]}@*)
(*@\supported{let po-rel = [M] ; po ; [Release]}@*)
(*@\supported{let po-unlock-rf-lock-po = po ; [UL] ; rf ; [LKR] ; po}@*)

(* Fences *)
let (*@\supported{R4rmb}@*) = R \ Noreturn	(* Reads for which rmb works *)
(*@\supported{let rmb = [R4rmb] ; fencerel(Rmb) ; [R4rmb]}@*)
(*@\supported{let wmb = [W] ; fencerel(Wmb) ; [W]}@*)
(*@\supported{let mb = ([M] ; fencerel(Mb) ; [M]) |}@*)
	(*@\supported{([M] ; fencerel(Before-atomic) ; [RMW] ; po? ; [M]) |}@*)
	(*@\supported{([M] ; po? ; [RMW] ; fencerel(After-atomic) ; [M]) |}@*)
	(*@\supported{([M] ; po? ; [LKW] ; fencerel(After-spinlock) ; [M]) |}@*)
	(*@\supported{([M] ; po ; [UL] ; (co | po) ; [LKW] ;}@*)
		(*@\supported{fencerel(After-unlock-lock) ; [M])}@*)
let gp = po ; [Sync-rcu | Sync-srcu] ; po?
let (*@\supported{strong-fence}@*) = (*@\supported{mb}@*) | gp

(*@\supported{let nonrw-fence = strong-fence | po-rel | acq-po}@*)
(*@\supported{let fence = nonrw-fence | wmb | rmb}@*)
(*@\supported{let barrier = fencerel(Barrier | Rmb | Wmb | Mb | Sync-rcu | Sync-srcu |}@*)
		(*@\supported{Before-atomic | After-atomic | Acquire | Release |}@*)
		(*@\supported{Rcu-lock | Rcu-unlock | Srcu-lock | Srcu-unlock) |}@*)
	(*@\supported{(po ; [Release]) | ([Acquire] ; po)}@*)

(**********************************)
(* Fundamental coherence ordering *)
(**********************************)

(* Sequential Consistency Per Variable *)
(*@\supported{let com = rf | co | fr}@*)
(*@\supported{acyclic po-loc | com as coherence}@*)

(* Atomic Read-Modify-Write *)
(*@\supported{empty rmw \& (fre ; coe) as atomic}@*)

(**********************************)
(* Instruction execution ordering *)
(**********************************)

(* Preserved Program Order *)
(*@\supported{let dep = addr | data}@*)
(*@\supported{let rwdep = (dep | ctrl) ; [W]}@*)
(*@\supported{let overwrite = co | fr}@*)
(*@\supported{let to-w = rwdep | (overwrite \& int) | (addr ; [Plain] ; wmb)}@*)
(*@\supported{let to-r = addr | (dep ; [Marked] ; rfi)}@*)
(*@\supported{let ppo = to-r | to-w | fence | (po-unlock-rf-lock-po \& int)}@*)

(* Propagation: Ordering from release operations and strong fences. *)
(*@\supported{let A-cumul(r) = (rfe ; [Marked])? ; r}@*)
(*@\supported{let cumul-fence = [Marked] ; (A-cumul(strong-fence | po-rel) | wmb |}@*)
	(*@\supported{po-unlock-rf-lock-po) ; [Marked]}@*)
(*@\supported{let prop = [Marked] ; (overwrite \& ext)? ; cumul-fence* ;}@*)
	(*@\supported{[Marked] ; rfe? ; [Marked]}@*)

(*
 * Happens Before: Ordering from the passage of time.
 * No fences needed here for prop because relation confined to one process.
 *)
let (*@\supported{hb}@*) = [Marked] ; (ppo | rfe | ((prop \ id) & int)) ; [Marked]
(*@\supported{acyclic hb as happens-before}@*)

(****************************************)
(* Write and fence propagation ordering *)
(****************************************)

(* Propagation: Each non-rf link needs a strong fence. *)
(*@\supported{let pb = prop ; strong-fence ; hb* ; [Marked]}@*)
(*@\supported{acyclic pb as propagation}@*)

(*******)
(* RCU *)
(*******)

(*
 * Effects of read-side critical sections proceed from the rcu_read_unlock()
 * or srcu_read_unlock() backwards on the one hand, and from the
 * rcu_read_lock() or srcu_read_lock() forwards on the other hand.
 *
 * In the definition of rcu-fence below, the po term at the left-hand side
 * of each disjunct and the po? term at the right-hand end have been factored
 * out.  They have been moved into the definitions of rcu-link and rb.
 * This was necessary in order to apply the "& loc" tests correctly.
 *)
let rcu-gp = [Sync-rcu]		(* Compare with gp *)
let srcu-gp = [Sync-srcu]
let rcu-rscsi = rcu-rscs^-1
let srcu-rscsi = srcu-rscs^-1

(*
 * The synchronize_rcu() strong fence is special in that it can order not
 * one but two non-rf relations, but only in conjunction with an RCU
 * read-side critical section.
 *)
let rcu-link = po? ; hb* ; pb* ; prop ; po

(*
 * Any sequence containing at least as many grace periods as RCU read-side
 * critical sections (joined by rcu-link) induces order like a generalized
 * inter-CPU strong fence.
 * Likewise for SRCU grace periods and read-side critical sections, provided
 * the synchronize_srcu() and srcu_read_[un]lock() calls refer to the same
 * struct srcu_struct location.
 *)
let rec rcu-order = rcu-gp | srcu-gp |
	(rcu-gp ; rcu-link ; rcu-rscsi) |
	((srcu-gp ; rcu-link ; srcu-rscsi) & loc) |
	(rcu-rscsi ; rcu-link ; rcu-gp) |
	((srcu-rscsi ; rcu-link ; srcu-gp) & loc) |
	(rcu-gp ; rcu-link ; rcu-order ; rcu-link ; rcu-rscsi) |
	((srcu-gp ; rcu-link ; rcu-order ; rcu-link ; srcu-rscsi) & loc) |
	(rcu-rscsi ; rcu-link ; rcu-order ; rcu-link ; rcu-gp) |
	((srcu-rscsi ; rcu-link ; rcu-order ; rcu-link ; srcu-gp) & loc) |
	(rcu-order ; rcu-link ; rcu-order)
let rcu-fence = po ; rcu-order ; po?
let fence = fence | rcu-fence
let strong-fence = strong-fence | rcu-fence

(* rb orders instructions just as pb does *)
let rb = prop ; rcu-fence ; hb* ; pb* ; [Marked]

irreflexive rb as rcu

(*
 * The happens-before, propagation, and rcu constraints are all
 * expressions of temporal ordering.  They could be replaced by
 * a single constraint on an "executes-before" relation, xb:
 *
 * let xb = hb | pb | rb
 * acyclic xb as executes-before
 *)

(*********************************)
(* Plain accesses and data races *)
(*********************************)

(* Warn about plain writes and marked accesses in the same region *)
let (*@\supported{mixed-accesses}@*) = ([Plain & W] ; (po-loc \ barrier) ; [Marked]) |
	([Marked] ; (po-loc \ barrier) ; [Plain & W])
flag ~empty (*@\supported{mixed-accesses}@*) as (*@\supported{mixed-accesses}@*)

(* Executes-before and visibility *)
let xbstar = (hb | pb | rb)*
let (*@\supported{vis}@*) = cumul-fence* ; rfe? ; [Marked] ;
	((strong-fence ; [Marked] ; xbstar) | (xbstar & int))

(* Boundaries for lifetimes of plain accesses *)
(*@\supported{let w-pre-bounded = [Marked] ; (addr | fence)?}@*)
let (*@\supported{r-pre-bounded}@*) = [Marked] ; (addr | nonrw-fence |
	([R4rmb] ; fencerel(Rmb) ; [~Noreturn]))?
(*@\supported{let w-post-bounded = fence? ; [Marked]}@*)
let (*@\supported{r-post-bounded}@*) = (nonrw-fence | ([~Noreturn] ; fencerel(Rmb) ; [R4rmb]))? ;
	[Marked]

(* Visibility and executes-before for plain accesses *)
let (*@\supported{ww-vis}@*) = fence | (strong-fence ; xbstar ; w-pre-bounded) |
	(w-post-bounded ; vis ; w-pre-bounded)
let (*@\supported{wr-vis}@*) = fence | (strong-fence ; xbstar ; r-pre-bounded) |
	(w-post-bounded ; vis ; r-pre-bounded)
let (*@\supported{rw-xbstar}@*) = fence | (r-post-bounded ; xbstar ; w-pre-bounded)

(* Potential races *)
let (*@\supported{pre-race}@*) = ext & ((Plain * M) | ((M \ IW) * Plain))

(* Coherence requirements for plain accesses *)
let (*@\supported{wr-incoh}@*) = pre-race & rf & rw-xbstar^-1
let (*@\supported{rw-incoh}@*) = pre-race & fr & wr-vis^-1
let (*@\supported{ww-incoh}@*) = pre-race & co & ww-vis^-1
(*@\supported{empty (wr-incoh | rw-incoh | ww-incoh) as plain-coherence}@*)

(* Actual races *)
let (*@\supported{ww-nonrace}@*) = ww-vis & ((Marked * W) | rw-xbstar) & ((W * Marked) | wr-vis)
let (*@\supported{ww-race}@*) = (pre-race & co) \ ww-nonrace
let (*@\supported{wr-race}@*) = (pre-race & (co? ; rf)) \ wr-vis
let (*@\supported{rw-race}@*) = (pre-race & fr) \ rw-xbstar

flag ~empty (*@\supported{(ww-race | wr-race | rw-race) as data-race}@*)
\end{lstlisting}

